// =============================================================================
// Bytecode Compiler
// =============================================================================

int32_t hdw_compile(hdw_compiler * const restrict compiler, hdw_treenode * const restrict tree) {
	
}
